apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: demo-d
  namespace: tx-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-d
  template:
    metadata:
      labels:
        app: demo-d
    spec:
      terminationGracePeriodSeconds: 60
#      hostNetwork: true
      containers:
      - name: demo-d
        image: 192.168.110.119:5000/yizhishang/demo-d
        imagePullPolicy: IfNotPresent
        # command: ["java"]
        # args: ["-jar", "/usr/local/demo-d.jar","--spring.profiles.active=test","--server.port=8300"]
        ports:
        - name: http
          containerPort: 8080
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /actuator/health
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 20
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /actuator/health
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 20
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
---

apiVersion: v1
kind: Service
metadata:
  name: demo-d
  namespace: tx-manager
  labels:
    app: demo-d
spec:
  selector:
    app: demo-d
  clusterIP: None
  ports:
    - name: http
      port: 8080
      protocol: TCP